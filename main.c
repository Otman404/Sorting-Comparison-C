#include <stdio.h>
#include <time.h>
#include <sys/time.h>
#include "tri.h"

int j;
int main()
{
    // int *t,n = 1000;
    //t = remplir(t,n);
    // afficher(t,n);
    //triFusion(t,0,n-1);

    // void (*p)(int *,int );

    // void (*t[])(int *,int) = {triBulle,triSelection,triInsertion};

    // printf("\n%f secs",getTemps(triRapide,t,n));

     evaluerTemps(10000);

    

    return 0;
}